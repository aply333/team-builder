import React from "react";

const FormTwo = (member) => {
    const addMember = {
        id:Date.now(),
        name: member.name,
        email: member.email,
        role: member.role
    };

    
}


    return(

    )
}